# Core dependencies for benchmark
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
python-dotenv>=1.0.0
tqdm>=4.65.0

# For visualization and reporting (optional)
plotly>=5.15.0
rich>=12.0.0

# Dependencies for the submodules (installed via pip install -e)
# The submodules have their own requirements.txt files that will be installed
# when you run:
# pip install -e external/argument-mining-api
# pip install -e external/argument-mining-db